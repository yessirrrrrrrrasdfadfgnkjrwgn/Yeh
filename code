local tsc = game:GetService("TextChatService")


local channels = tsc:WaitForChild("TextChannels")

local general = channels:WaitForChild("RBXGeneral")

while true do
	wait(0.2)
	general:SayAsync("im so tight")
 end
